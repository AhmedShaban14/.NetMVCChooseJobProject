@model IEnumerable<TemplateProject.Models.Job>


@*<table class="table table-hover table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.User.UserName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.JobTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.JobContent)
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                <h5>@Html.DisplayFor(modelItem => item.User.UserName)</h5>
            </td>
            <td>
                <h5>@Html.DisplayFor(modelItem => item.JobTitle)</h5>
            </td>
            <td>
                <h5>@Html.DisplayFor(modelItem => item.JobContent)</h5>
            </td>
        </tr>
        <tr>
            @foreach (var sub in item.ApplyJob)
                {
            <tr>
                <td>@sub.User.UserName</td>
                <td>@sub.Message</td>
                <td>@sub.Date</td>
            </tr>
                }
            </tr>
    }
</table>*@

    @foreach (var item in Model)
    {

        <div class="row bg-light navbars">
            <div class="col-lg-3">
                @Html.DisplayFor(modelItem => item.JobTitle)
            </div>
            <div class="col-lg-3">
                @Html.Raw(HttpUtility.HtmlDecode(Html.DisplayFor(modelItem => item.JobContent).ToString()))
            </div>
            <div class="col-lg-6">
                <ul class="ulClass list-unstyled">
                    <li>@Html.ActionLink("Edit", "", new { id = item.Id }, new { @class = "btn btn-success" })</li>
                    <li>@Html.ActionLink("Details", "", new { id = item.Id }, new { @class = "btn btn-success" })</li>
                    <li>@Html.ActionLink("Delete", "", new { id = item.Id }, new { @class = "btn btn-success" })</li>
                </ul>
            </div>
        </div>
            foreach (var sub in item.ApplyJob)
            {
                    <ul class="list-group"  >
                       <strong>User Name :</strong>    <li class="list-group-item col-md-3">@sub.User.UserName</li>
                        <strong>User Message : </strong>   <li class="list-group-item col-md-3">@sub.Message</li>
                        <strong>User Date : </strong> <li class="list-group-item col-md-3">@sub.Date</li>
                    </ul>
                   
                    <hr   />
            }

    }







